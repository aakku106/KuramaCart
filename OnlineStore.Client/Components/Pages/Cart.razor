@page "/kitchen"
<PageTitle>KuramaCart🦊 - Kitchen Items 🧑🏻‍🍳</PageTitle>
<h3>Shopping Cart</h3>

<div class="container">
    <div class="page-header">
        <h1 class="page-title">Kitchen Products 💄</h1>
        <div class="product-count">
            Showing @beauty.kitchenProducts.Count() Products 👀
        </div> Your cart is empty.
    </div>
</div>
}
<h3>Kitchen Products</h3>
{
@if (products == null) ms.Any())
{ {
<p><em>Loading...</em></p>b-4">
} <div class="card-header">
    else if (!products.Any())y Products
    { </div>
<p>No kitchen products found.</p>">
} <table class="table">
    else <thead>
        { <tr>
            <div class="row">
                <th>Product</th>
                @foreach (var product in products)/ th >
                { <th>Action</th>
                <div class="col-md-4 mb-4">
                    <div class="card">
                        <img src="@product.ImagePath" class="card-img-top" alt="@product.ProductName">
                        <div class="card-body">tem in beautyItems)
                            <h5 class="card-title">@product.ProductName</h5>
                            <p class="card-text">₹@product.Price</p>
                            <button @onclick="@(() => AddToCart(product))" class="btn btn-primary">Add to Cart</button>
                        </div>
                        <td>₹@item.Price</td>
                    </div>
                    <td>
                </div> <button class="btn btn-sm btn-danger" @onclick="@(() => RemoveBeautyItem(item))">Remove</button>
                } </td>
            </div>
</tr>
        } }
</div>
</tbody>
</table>
<div class="modal fade" id="exampleModal" tabindex="-1" aria-labelledby="exampleModalLabel" aria-hidden="true">
    <div class="modal-dialog modal-dialog-centered">
        <div class="modal-content">
            <div class="modal-header">
                <h1 class="modal-title fs-5" id="exampleModalLabel">Add to Cart 🛒</h1>
                <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"
                    @onclick="Clear"></button>
            </div>
            <div class="card-header">
                <div class="modal-body">ucts
                    <p class="text-center mb-0" style="color: #2d3436; font-size: 1.1rem;">
                        @if (!isOutOfStock) dy">
                        {
                    <table class="table">
                        <span>head>
                            Are you sure you want to add "@selectedProduct?.ProductName" to your cart? 🤔
                        </span>
                        <th>Product</th>
                        } <th>Price</th>
                        else <th>Action</th>
                        { </tr>
                        <span>thead>
                            🦊 @productDetailsUIMessage 🤧
                        </span> @foreach (var item in kitchenItems)
                        } {
                        </p>
                        <tr>
                </div>
                <td>@item.ProductName</td>
                <div class="modal-footer">
                    <td>₹@item.Price</td>
                    @if (isOutOfStock)
                        <td>
                            { <button class="btn btn-sm btn-danger"
                                @onclick="@(() => RemoveKitchenItem(item))">Remove</button>
                            <button type="button" class="btn btn-secondary" data-bs-dismiss="modal" @onclick="Clear">
                                Okii 🥲 </tr>
                    
                            </button> }
                            } </tbody>
                            else </table>
                            {
                    </div>
                    <button type="button" class="btn btn-secondary" data-bs-dismiss="modal" @onclick="Clear">
                        Cancel
                    </button>
                    <button type="button" class="btn btn-primary" data-bs-dismiss="modal" @onclick="AddToCart">
                        Yes, Add to Cart! 🦊
                    </button>
                    }s="card">
                </div>lass="card-header">
            </div> Order Summary
        </div>
    </div>
    </div>
    <div class="card-body">
        <p>Total: ₹@totalAmount</p>
        <button class="btn btn-primary w-100" @onclick="GoToCheckout"
            disabled="@(!kitchenItems.Any() && !beautyItems.Any())">
            Checkout
        </button>
    </div>
    </div>
    </div>
    </div>
